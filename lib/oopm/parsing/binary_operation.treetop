grammar BinaryOperation
  include AnyObject
  include Whitespace

  rule binary_operation
    (any_object whitespace binary_operator whitespace any_object)
  end

  rule binary_operator
    ("+" / "-" / "*" / "/" / "%" / "||" / "&&" / "==" / "|" / "&")
  end
end
